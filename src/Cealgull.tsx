import { Text } from "react-native";
import { SafeAreaProvider } from "react-native-safe-area-context";

export default function Cealgull() {
  return (
    <SafeAreaProvider>
      <Text>Cealgull App</Text>
    </SafeAreaProvider>
  );
}
